import React from "react";
import { createRoot } from "react-dom/client";
import CTABuilder from "../CTABuilder.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CTABuilder />
  </React.StrictMode>
);
